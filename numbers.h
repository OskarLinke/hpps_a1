#include "bits.h"
#include <stdbool.h>
#include <stdio.h>
#include <assert.h>

struct bits8{
    struct bit b0; 
    struct bit b1; 
    struct bit b2; 
    struct bit b3; 
    struct bit b4; 
    struct bit b5; 
    struct bit b6; 
    struct bit b7;
}; 



